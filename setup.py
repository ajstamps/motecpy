"""python-can requires the setuptools package to be installed."""from setuptools import setup, find_packages__version__ = "1.0.1"import logginglogging.basicConfig(level=logging.WARNING)setup(    name="motecpy",    url="https://github.com/ajstamps/MotecPy/",    version=__version__,    packages=find_packages(),    author="Alex Stamps",    author_email="hardbyte@gmail.com",    description="motec ecu interface module for Python using can, and PiCAN2 Hat",    long_description=open('README.md').read(),    license="None",    scripts=["./motecpy.py"],    # Tests can be run using `python setup.py test`    test_suite="nose.collector",    tests_require=['mock'],    install_requires=['can'])